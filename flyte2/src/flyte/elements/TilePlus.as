package flyte.elements{	import flyte.core.Body;	import flyte.events.CollisionEvent;	import flyte.events.GameEvent;	import flyte.utils.CollisionDictionaryOld;		public class TilePlus extends Tile    {        private var collision:CollisionDictionaryOld                public function TilePlus()        {                      collision = new CollisionDictionaryOld(this)                event.onLoopDo(update)                addEventListener(CollisionEvent.COLLIDE,onCollision)        }                private function onCollision(e:CollisionEvent):void        {        }                private function update(e:GameEvent):void        {        }    }}